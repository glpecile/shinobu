/**
 * Native: no host needed — burnt renders iOS banners / Android toasts from
 * native code. The web sibling mounts sonner's `<Toaster />` (plan 0032 U1).
 */
export function ToastHost() {
  return null;
}
