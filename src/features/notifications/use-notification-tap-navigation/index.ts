/**
 * Web schedules and receives no notifications (R12), so there is nothing to
 * tap through — `index.native.ts` carries the real listener.
 */
export function useNotificationTapNavigation(): void {}
