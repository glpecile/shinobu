.idea

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Continuous Native Generation - regenerated via `expo prebuild`, never hand-edited
/android
/ios

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*
!.env.example

# typescript
*.tsbuildinfo
# src/uniwind-types.d.ts is generated by uniwind's bundler integration but is
# committed on purpose: CI's typecheck needs it and can't boot Metro to
# regenerate it. If themes change, the dev server rewrites it — commit the diff.
# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
